# /patterns-detect — Weekly pattern surfacing via pattern-detector agent

Run the pattern-detector agent contract (see `_agents/pattern-detector.md`):
1. Read `brain/notes/` and `brain/_inbox/` from the last 30 days.
2. Identify recurring topics, emerging themes, decision velocity, anti-patterns, open loops.
3. Write `brain/patterns/{YYYY}-W{ww}.md` per the weekly template.
4. Refresh `brain/_moc/MOC-Patterns.md` with the latest week.

Rules:
- Show your work. Every claim links to source notes.
- Counts are exact (or say "approximately").
- Never infer about psychology, relationships, or values (that's paid-tier territory).
- End with the SIP attestation block.

Built on SIP.
